#include<stdio.h>
#include<stdlib.h>
#include<string.h>
void printCharacter(string str)
{
int len=str.size();
int middle=len/2;
printf("/n");
int temp;
temp=str(middle);
for(int i=0;i<=len;i++)
{
for(int k=0;k<=i;k++)
{
printf("%s",str(temp));
for(int l=2;l<=k;l++)
{
 printf(str(temp+l);
}
printf("/n");
}
}
}
int main()
{
printf("enter string");
scanf("%s",&str);
printCharacter(str);
return 0;
}
